{% extends 'base.html.twig' %}

{% block title %}LineOrder{% endblock %}

{% block body %}
    <h1>LineOrder</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ line_order.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ line_order.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('line_order_index') }}">back to list</a>

    <a href="{{ path('line_order_edit', {'id': line_order.id}) }}">edit</a>

    {{ include('line_order/_delete_form.html.twig') }}
{% endblock %}
